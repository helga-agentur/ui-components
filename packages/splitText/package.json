{
  "name": "@helga-agency/splittext",
  "version": "1.6.2",
  "description": "Splits textContent of an HTML element into multiple parts",
  "main": "splitText.js",
  "types": "splitText.d.ts",
  "scripts": {
    "test": "npx ava --verbose",
    "build": "npx rollup --c rollup.config.mjs",
    "lint": "npx eslint \"src/**/*.{js,mjs}\" -c ../../node_modules/@joinbox/eslint-config-joinbox/index.js"
  },
  "author": "Felix Steiner <felix@helga.ch>",
  "license": "ISC",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@helga-agency/ui-tools":"*"
  }
}
